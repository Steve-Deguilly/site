{
  "name": "steve-deguilly-site",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "@astrojs/sitemap": "^3.2.0",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^5.0.0",
    "dompurify": "^3.4.7",
    "marked": "^18.0.4",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "wrangler": "^4.105.0"
  }
}
